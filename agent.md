# CISLC-O3 Agent Guide

## 目的
- 本文件服务于后续 agent / 协作者进入仓库时的执行约束。
- 本文件不承担“当前实现状态索引”的职责。
- 后端模块现状、数据流、时序入口、受影响 RTL 路径，保留在 [`doc/CISLC_O3.md`](/home/chen/work/CISLC-O3/doc/CISLC_O3.md)。
- 前端模块现状、数据流、时序入口、受影响 RTL 路径，保留在 [`doc/CISLC_O3_frontend.md`](/home/chen/work/CISLC-O3/doc/CISLC_O3_frontend.md)。

## 与 `doc/CISLC_O3.md` 的分工
适合放在本文件中的内容：
- 面向后续修改者的工作规则。
- 当前阶段的范围边界，例如“先搭最小核心闭环”或“只做方案不改代码”。
- RTL 注释规范。
- 修改顺序建议、阅读顺序建议、提交前自检项。
- 文档维护约定，例如改完 RTL 后必须同步更新主文档。

应该保留在实现状态文档中的内容：
- 当前实现状态。
- 当前后端数据流。
- 当前前端数据流。
- 各模块职责、已实现项、未实现项。
- 逐周期时序说明。
- 已知限制。
- 后续扩展入口。
- RTL 文件索引与模块关系。

判断原则：
- 如果一段内容描述的是“这个项目现在是什么样”，应留在 `doc/CISLC_O3.md`。
- 如果一段内容描述的是“后续 agent 应该怎么做事”，应放在本文件。

## 建议阅读顺序
1. 先按任务方向选择实现状态文档：
   - 后端任务先读 [`doc/CISLC_O3.md`](/home/chen/work/CISLC-O3/doc/CISLC_O3.md)。
   - 前端任务先读 [`doc/CISLC_O3_frontend.md`](/home/chen/work/CISLC-O3/doc/CISLC_O3_frontend.md)。
2. 再读与任务相关的 RTL。
3. 最后按本文件的约束执行修改。

## 当前阶段工作边界
- 当前阶段是乱序框架重构期，不以旧工程保持可编译、旧测试保持通过或尽快形成最小闭环为目标。
- 按照指令真实运行方向，从 Frontend → Decode → Rename → IEW → Commit 依次梳理接口、职责、状态所有权和周期行为；只有用户确认当前位置已经讲清并改完，才继续到下一个位置。
- 旧模块、旧接口和旧测试随着重构路径推进再逐个迁移。尚未轮到的下游代码允许暂时引用旧字段、无法编译或测试失败，不得为维持兼容而加入过渡字段或额外适配层。
- 当前阶段不要求新增定向测试或运行既有回归。等整条指令路径完成全局梳理后，再统一迁移、修正和补充测试，并进行全局验证。
- 若用户单独要求检查某项测试或验证某段路径，则把它作为独立任务执行并明确证据边界；不得据此改变当前重构顺序。
- 保持现有设计风格，不主动引入另一套抽象层次或命名体系。
- 前端和后端统一执行同一套约束：模块头注释、关键逻辑注释、逐周期时序说明都必须补齐。
- 如果修改影响到模块接口、状态机、映射表、队列、握手或周期级行为，必须同步更新对应的实现状态文档。

## 长期微架构开发工作流

本项目是一个以长期理解、亲手建立和可维护性为目标的乱序核项目，不以招聘、短期展示或一次性“完成整核”为推进标准。后续协作者必须遵守以下工作流。

### 沿指令运行方向一次只推进一个目标

- 推进顺序由用户掌握。协作者必须跟随用户当前正在梳理的位置，不得自行跳到下游模块、测试收口或全局修复。
- 每次只选择指令运行路径上一个边界清楚的接口、模块、机制或硬件不变量，例如 Frontend→Decode 的 entry 合同；不得同时扩展到尚未轮到的 IQ、LSQ、MMU 或完整 FASE 集成。
- 当前目标未完成前，远期功能只能保留接口、元数据或扩展位置，不得借机扩大实现范围。
- 如果在实现中发现必须修改上游或下游契约，应先停止修改，向用户解释原因、新的范围和对已有设计的影响。

### 固定推进顺序

当前所处的每个接口、模块或机制按以下顺序推进：

1. 用中文明确它负责什么、不负责什么。
2. 写出必须始终成立的硬件不变量，包括年龄顺序、资源所有权、原子更新和不得重复处理等约束。
3. 定义接口、参数、数据结构与 ready/valid 握手。
4. 说清周期 N 的组合行为、周期 N 上升沿的状态更新和周期 N+1 的可见结果。
5. 用最小 RTL 实现已经批准的合同，不同时引入未讨论的优化。
6. 做静态检查并同步实现状态文档，记录本段已经确定的合同、尚未迁移的旧引用和下一个顺序位置；静态检查不等同于功能验证。
7. 向用户解释本段代码如何承接上游、如何交给下游，以及当前仍未验证什么；在用户确认后再沿指令方向进入下一位置。

整条指令路径完成全局梳理后，再进入测试阶段：统一迁移旧 testbench 和位段解析，补充定向测试，运行相关回归，最后处理全局集成问题。框架重构期不得用“测试必须通过”阻断顺序推进，也不得把未运行测试的代码表述为已经通过功能验证。

### 协作与可理解性边界

- 用户负责决定推进节奏和微架构取舍；协作者负责跟随该节奏，把决定整理成可检查的合同，依次实现 RTL、维护文档，并清楚标注静态检查与功能验证的证据边界。
- 不得交付无法说明周期行为、状态所有权或正确性依据的黑盒代码。
- 不得以“代码已改”代替“路径通过验证”。框架重构期可以记录某段合同或迁移已经完成，但未进入统一测试阶段前，不得宣称功能验证通过。
- 可以慢、可以重写，但每次重写必须指明被替代的合同、新的不变量和重新验证的范围。

### 每次交付的固定记录

每次完成一个重构位置时，最终说明和对应实现状态文档至少必须记录：

- 本次固化了哪些接口、周期行为和硬件不变量。
- 本次只做了哪些静态检查，哪些功能测试明确尚未执行或需要等待全局梳理后统一更新。
- 哪些旧模块、旧接口或旧测试仍未迁移，以及这种暂时失效是否符合当前顺序。
- 当前仍然没有实现什么，不得用预留接口暗示功能已经完成。
- 沿指令运行方向的下一个位置是什么，以及哪些下游功能继续保持冻结。

## 修改前协作规则
- 任何代码或文档修改前，必须先向用户提出修改方案。
- 修改方案必须说明：
  - 准备改哪些文件。
  - 每个文件为什么需要修改。
  - 修改会影响哪些接口、状态机、数据流、握手或文档章节。
  - 是否需要同步更新实现状态文档。
- 在用户明确批准前，不允许执行实际文件修改。
- 如果用户只要求检查、分析、 review 或讨论方案，不要默认进入修改阶段。
- 如果发现当前方案需要扩大修改范围，必须先停下来补充说明并重新等待用户批准。

## RTL 修改规则
- 先补功能链路，再补更完整的验证。
- 按指令运行方向重构当前选定位置，不以维持旧代码全局闭环为目标；不提前引入尚未轮到的 ROB、commit、rollback、flush 等后续机制，除非任务明确要求。
- 对当前阶段未实现的能力，要在模块注释中明确写出，不要用隐含假设代替。
- 对同拍多 lane 关系、覆盖优先级、旁路、资源冲突，如果本次没有处理，必须直接写清楚“当前不处理”。
- 除模块头注释外，代码内部的关键逻辑块也必须写解释性注释，说明“这一段在做什么”和“为什么这样做”，便于后续手动调参、时序优化和物理实现阅读。

## 模块头注释要求
每次新增或修改 RTL，模块头注释至少写清楚：
- 当前已经实现什么功能。
- 当前没有实现什么功能。
- 未来准备从哪里扩展。
- 本阶段是否故意不写测试代码和仿真代码。

## 逐周期说明要求
只要模块里有以下任一种内容，就必须补逐周期说明：
- 状态寄存器。
- 队列或表项存储。
- ready/valid 或类似握手。
- 多拍可见的映射关系。

逐周期说明至少覆盖三件事：
- 周期 N 组合阶段输出什么。
- 周期 N 上升沿更新什么状态。
- 周期 N+1 能看到什么。

禁止事项：
- 不要使用字符串画波形。
- 不要只写“见代码可知”。
- 不要把关键时序信息只散落在行内注释里。
- 也不要只写文件头注释而不给代码关键块写注释。

## 文档维护规则
- 若新增或修改的是后端 RTL，需同步更新 [`doc/CISLC_O3.md`](/home/chen/work/CISLC-O3/doc/CISLC_O3.md)。
- 若新增或修改的是前端 RTL，需同步更新 [`doc/CISLC_O3_frontend.md`](/home/chen/work/CISLC-O3/doc/CISLC_O3_frontend.md)。
- 若 RTL 改变数据流，需更新对应文档中的数据流章节。
- 若 RTL 改变周期级行为，需更新对应文档中的关键时序行为章节。
- 若本次仅修改协作规范、执行策略、文档分工，应优先更新本文件。

## 固定回归要求
- 本节只在用户明确要求运行测试、检查回归或当前工作已经进入统一测试阶段时生效；框架重构期不默认执行，也不要求这些测试保持通过。
- 当前仿真已经收敛为一个完整 `o3_core` Verilator入口。用户要求检查当前整核仿真或Tandem退休输出时，运行：

```bash
cd sim/o3
make test
```

- `sim/o3` 是当前唯一仿真入口：外部C++驱动真实Frontend/Backend和ICache refill，并输出按ROB退休顺序排列的JSONL记录。
- 当前结果只证明Tandem trace producer能够运行，不表示已经与Spike完成差分验证。
- 如果该入口失败，最终回复必须区分Verilator编译、ICache refill、核心超时和退休记录错误。

## 建议保留在主文档中的代码索引
以下信息建议长期保留在 [`doc/CISLC_O3.md`](/home/chen/work/CISLC-O3/doc/CISLC_O3.md)，不要外移：
- `rtl/backend/backend.sv`：后端最小 rename 链路总装。
- `rtl/backend/decoder.sv`：最小 RVI rename 语义解码。
- `rtl/backend/free_list.sv`：空闲物理寄存器分配。
- `rtl/backend/rename_map_table.sv`：架构寄存器到物理寄存器映射。
- `rtl/backend/physical_regfile.sv`：物理寄存器文件实现与后续接入口。
- `rtl/common/o3_pkg.sv`：跨模块数据结构定义。
- `rtl/core/o3_core.sv`：当前真实 frontend + backend 的 core 级连接入口。
- `sim/o3/`：当前唯一的完整core Verilator与Tandem退休轨迹入口。

以下信息建议长期保留在 [`doc/CISLC_O3_frontend.md`](/home/chen/work/CISLC-O3/doc/CISLC_O3_frontend.md)，不要外移：
- `rtl/frontend/frontend.sv`：前端当前实现入口。
- `rtl/O3.sv`、`rtl/Tile.sv`：前端最终会经过的系统级连接入口。

## 修改前自检
- 任务影响的是“协作规则”还是“实现状态”？
- 需要改的是 `agent.md`，还是 `doc/CISLC_O3.md`，还是两者都要改？
- 是否已经读过相关 RTL，而不是只看文档就下结论？

## 修改后自检
- 主文档是否仍然能作为代码索引使用？
- agent 文档是否只保留规则，不重复堆叠实现细节？
- 模块注释和逐周期说明是否与 RTL 真实行为一致？
